---
interface Props {
  symbol?: string;
  value?: string;
}

const { symbol = "$", value = "1.00" } = Astro.props;
---

<div class="flex items-center gap-4 rounded-2xl bg-card p-5">
  <span class="text-2xl font-light text-white/60">{symbol}</span>
  <input
    type="text"
    value={value}
    readonly
    class="flex-1 bg-transparent text-3xl font-bold text-white outline-none"
  />
</div>
