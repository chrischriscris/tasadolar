---
import { cn } from '../../lib/utils';

interface Props extends astroHTML.JSX.HTMLAttributes {
  class?: string;
}

const { class: className, ...props } = Astro.props;
---

<div
  data-slot="card-content"
  class={cn("px-4", className)}
  {...props}
>
  <slot />
</div>