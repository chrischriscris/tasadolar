---
import "../styles/global.css";

const cloudflareWebAnalyticsToken = import.meta.env
  .PUBLIC_CLOUDFLARE_WEB_ANALYTICS_TOKEN;

interface Props {
  title?: string;
  description?: string;
  canonicalPath?: string;
  image?: string;
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
  tabFiltering?: boolean;
}

const {
  title = "TasaDolar | Convertidor USD, USDT y bolívares en Venezuela",
  description = "Consulta la tasa del dólar BCV, Binance USDT y euro BCV en Venezuela. Convierte USD, USDT y bolívares con tasas actualizadas.",
  canonicalPath = "/",
  image = "/icons/icon-512x512.png",
  jsonLd,
  tabFiltering = false,
} = Astro.props;

const siteUrl =
  import.meta.env.PUBLIC_SITE_URL ?? Astro.site?.href ?? Astro.url.origin;
const canonicalUrl = new URL(canonicalPath, siteUrl).href;
const imageUrl = new URL(image, siteUrl).href;
const jsonLdText = jsonLd ? JSON.stringify(jsonLd) : undefined;
---

<!doctype html>
<html
  lang="es-VE"
  class="dark h-full overflow-hidden"
  data-active-tab="USD"
  data-tab-filtering={tabFiltering ? "true" : undefined}
>
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="/icons/icon-16x16.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/icons/icon-32x32.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="192x192"
      href="/icons/icon-192x192.png"
    />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow, max-image-preview:large" />
    <meta name="googlebot" content="index, follow, max-image-preview:large" />
    <link rel="canonical" href={canonicalUrl} />
    <meta name="theme-color" content="#0a0a0a" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-title" content="TasaDolar" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="es_VE" />
    <meta property="og:site_name" content="TasaDolar" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={imageUrl} />
    <meta
      property="og:image:alt"
      content="TasaDolar convertidor de dólares, USDT y bolívares"
    />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    <script is:inline>
      try {
        const stored = localStorage.getItem("tasadolar:converter");
        const activeTab = stored ? JSON.parse(stored).activeTab : "USD";
        if (["USD", "EUR", "BS"].includes(activeTab)) {
          document.documentElement.dataset.activeTab = activeTab;
        }
      } catch {
        document.documentElement.dataset.activeTab = "USD";
      }
    </script>
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-640x1136.png"
      media="(device-width: 320px) and (device-height: 568px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-750x1334.png"
      media="(device-width: 375px) and (device-height: 667px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-828x1792.png"
      media="(device-width: 414px) and (device-height: 896px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1125x2436.png"
      media="(device-width: 375px) and (device-height: 812px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1170x2532.png"
      media="(device-width: 390px) and (device-height: 844px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1179x2556.png"
      media="(device-width: 393px) and (device-height: 852px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1242x2208.png"
      media="(device-width: 414px) and (device-height: 736px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1242x2688.png"
      media="(device-width: 414px) and (device-height: 896px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1284x2778.png"
      media="(device-width: 428px) and (device-height: 926px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1290x2796.png"
      media="(device-width: 430px) and (device-height: 932px) and (-webkit-device-pixel-ratio: 3)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1536x2048.png"
      media="(device-width: 768px) and (device-height: 1024px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1620x2160.png"
      media="(device-width: 810px) and (device-height: 1080px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1668x2224.png"
      media="(device-width: 834px) and (device-height: 1112px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-1668x2388.png"
      media="(device-width: 834px) and (device-height: 1194px) and (-webkit-device-pixel-ratio: 2)"
    />
    <link
      rel="apple-touch-startup-image"
      href="/splash/apple-splash-2048x2732.png"
      media="(device-width: 1024px) and (device-height: 1366px) and (-webkit-device-pixel-ratio: 2)"
    />
    <title>{title}</title>
    <link rel="preconnect" href="https://flagcdn.com" />
    <link rel="preconnect" href="https://public.bnbstatic.com" />
    {jsonLdText && <script type="application/ld+json" set:html={jsonLdText} />}
  </head>
  <body
    class="bg-background text-foreground h-dvh min-h-screen overflow-hidden antialiased"
  >
    <slot />
    {
      cloudflareWebAnalyticsToken && (
        <script
          defer
          src="https://static.cloudflareinsights.com/beacon.min.js"
          data-cf-beacon={JSON.stringify({
            token: cloudflareWebAnalyticsToken,
          })}
        />
      )
    }
    <script is:inline>
      const refreshIndicator = document.querySelector(
        "[data-refresh-indicator]",
      );
      const freshReloadKey = "tasadolar:fresh-reload-used";
      let reloadPending = false;

      const setRefreshIndicatorVisible = (visible) => {
        if (refreshIndicator) refreshIndicator.hidden = !visible;
      };

      const refreshCachedPage = () => {
        if (!navigator.onLine) return;

        setRefreshIndicatorVisible(true);
        if (navigator.serviceWorker.controller) {
          navigator.serviceWorker.controller.postMessage({
            type: "refresh-page",
          });
        } else {
          window.location.reload();
        }
      };

      const reloadWithFreshPage = () => {
        if (reloadPending || sessionStorage.getItem(freshReloadKey)) {
          setRefreshIndicatorVisible(false);
          return;
        }

        reloadPending = true;
        sessionStorage.setItem(freshReloadKey, "true");
        setRefreshIndicatorVisible(true);
        window.setTimeout(() => window.location.reload(), 150);
      };

      window.addEventListener("online", () => {
        refreshCachedPage();
      });
      window.addEventListener("offline", () => {
        setRefreshIndicatorVisible(false);
      });

      if ("serviceWorker" in navigator) {
        navigator.serviceWorker.addEventListener("message", (event) => {
          if (event.data?.type === "cached-page-used") {
            if (navigator.onLine) {
              setRefreshIndicatorVisible(true);
            }
          }

          if (event.data?.type === "fresh-page-ready") {
            reloadWithFreshPage();
          }

          if (event.data?.type === "fresh-page-current") {
            sessionStorage.removeItem(freshReloadKey);
            setRefreshIndicatorVisible(false);
          }

          if (event.data?.type === "refresh-page-failed") {
            setRefreshIndicatorVisible(false);
          }
        });

        navigator.serviceWorker.register("/sw.js");
      }
    </script>
  </body>
</html>
